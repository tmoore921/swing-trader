# Copy to .env and fill in values
# No credentials needed — market data comes from yfinance (free)
# Robinhood order execution is handled by the CCR agent via MCP

# Optional: override account size if it changes
ACCOUNT_SIZE=500
